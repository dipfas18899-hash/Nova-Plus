rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper Functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }
    
    function hasRequiredFields(fields) {
      return request.resource.data.keys().hasAll(fields);
    }

    // Users Collection
    match /users/{userId} {
      // Anyone authenticated can read user profiles to find people to chat/call with
      allow read: if isAuthenticated();
      
      // Only the user can create or update their own profile
      allow create: if isOwner(userId) 
                    && hasRequiredFields(['uid', 'email', 'displayName', 'status'])
                    && request.resource.data.uid == userId;
                    
      allow update: if isOwner(userId)
                    && (!('uid' in request.resource.data) || request.resource.data.uid == userId);
    }

    // Chats Collection
    match /chats/{chatId} {
      // Helper to check if the user is part of this chat
      function isParticipant() {
        let isExistingParticipant = resource != null && 'participants' in resource.data && request.auth.uid in resource.data.participants;
        let isNewParticipant = request.resource != null && 'participants' in request.resource.data && request.auth.uid in request.resource.data.participants;
        return isAuthenticated() && (isExistingParticipant || isNewParticipant || chatId.matches('.*' + request.auth.uid + '.*'));
      }
      
      // Participants can read and write chat metadata
      allow read, write: if isParticipant();

      // Messages Subcollection
      match /messages/{messageId} {
        // Participants can read messages
        allow read: if isAuthenticated() && chatId.matches('.*' + request.auth.uid + '.*');
        
        // Sender can create messages
        allow create: if isAuthenticated() 
                      && chatId.matches('.*' + request.auth.uid + '.*')
                      && request.resource.data.senderId == request.auth.uid
                      && hasRequiredFields(['senderId', 'receiverId', 'timestamp'])
                      && (request.resource.data.keys().hasAny(['text', 'audioUrl', 'gifUrl']));
      }
    }

    // Calls Collection
    match /calls/{callId} {
      function isCallParticipant() {
        let isExistingParticipant = resource != null && (request.auth.uid == resource.data.callerId || request.auth.uid == resource.data.receiverId);
        let isNewParticipant = request.resource != null && (request.auth.uid == request.resource.data.callerId || request.auth.uid == request.resource.data.receiverId);
        return isAuthenticated() && (isExistingParticipant || isNewParticipant);
      }

      // Caller or receiver can read and update the call
      allow read, update: if isCallParticipant();
      
      // Caller can create the call
      allow create: if isAuthenticated() 
                    && request.resource.data.callerId == request.auth.uid
                    && hasRequiredFields(['callerId', 'receiverId', 'status']);

      // WebRTC Signaling Subcollections
      match /offerCandidates/{candidateId} {
        allow read, write: if isAuthenticated();
      }
      
      match /answerCandidates/{candidateId} {
        allow read, write: if isAuthenticated();
      }
    }

    // File Transfers Collection
    match /file_transfers/{transferId} {
      allow read, update, delete: if isAuthenticated() && (
        request.auth.uid == resource.data.senderId || 
        request.auth.uid == resource.data.receiverId
      );
      
      allow create: if isAuthenticated() && request.resource.data.senderId == request.auth.uid;

      // WebRTC Signaling Subcollections
      match /offerCandidates/{candidateId} {
        allow read, write: if isAuthenticated();
      }
      
      match /answerCandidates/{candidateId} {
        allow read, write: if isAuthenticated();
      }
    }
  }
}
